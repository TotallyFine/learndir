#!/bin/bash

# 第一行的#!代表这是一个shell脚本，标识了这个脚本由哪个shell解释
echo "Hell World"

# 可以通过bash name.sh来执行脚本,此时是文件名作为了shell解释器的参数
# 也可以通过./name.sh，后者需要赋予权限
# chmod a+x name.sh     ./name.sh

# shell的编写流程
# 1 编写shell脚本
# 2 赋予可执行权限
# 3 执行调试
